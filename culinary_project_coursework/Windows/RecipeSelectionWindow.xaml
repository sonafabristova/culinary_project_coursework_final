<Window x:Class="culinary_project_coursework.Windows.RecipeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор рецепта" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Выберите рецепт" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <ListBox Grid.Row="1" 
                 Name="RecipesListBox" 
                 Margin="0,0,0,15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" 
                            BorderThickness="1" 
                            Margin="2" 
                            Padding="10">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" 
                                      FontWeight="Bold"/>
                            <TextBlock Text="{Binding Description}" 
                                      FontStyle="Italic" 
                                      TextWrapping="Wrap"
                                      Margin="0,5,0,0"/>
                            <TextBlock Text="{Binding CookingTime, StringFormat='Время: {0} мин'}" 
                                      Foreground="Blue"
                                      Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding Calories, StringFormat='Калории: {0} ккал'}" 
                                      Foreground="Red"
                                      Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Выбрать" 
                    Click="SelectButton_Click" 
                    Padding="15,8" 
                    Margin="0,0,10,0"/>
            <Button Content="Отмена" 
                    Click="CancelButton_Click" 
                    Padding="15,8"/>
        </StackPanel>
    </Grid>
</Window>